{
  "name": "websocket-server",
  "version": "1.0.0",
  "description": "Mock WebSocket Server for CRM system",
  "main": "websocket-server.js",
  "scripts": {
    "start": "node websocket-server.js",
    "dev": "tsx websocket-server.ts",
    "build": "tsc websocket-server.ts"
  },
  "dependencies": {
    "ws": "^8.14.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/ws": "^8.5.10",
    "tsx": "^4.6.0",
    "typescript": "^5.3.0"
  }
}